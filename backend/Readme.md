# Istruzioni per avviare node

- Assicurarsi di avere Node e MongoDb installati
- Fare `sudo systemctl start mongod` per avviare MongoDb (non più necessario)
- Fare `npm install` per installare i moduli node
- Fare `npm run server`
